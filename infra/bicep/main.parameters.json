{
  "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentParameters.json#",
  "contentVersion": "1.0.0.0",
  "parameters": {
    "environmentName": {
      "value": "${AZURE_ENV_NAME}"
    },
    "location": {
      "value": "${AZURE_LOCATION}"
    },
    "deploymentEnvironment": {
      "value": "${PLATFORM_DEPLOYMENT_ENVIRONMENT=development}"
    }
  },
  "//logRetentionDays": {
    "note": "Left to the template default. azd substitutes environment variables as strings, and ARM rejects a string for an int parameter — numeric overrides belong in a per-environment .bicepparam file (Milestone 06)."
  }
}
