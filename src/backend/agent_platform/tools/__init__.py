"""Tool implementations.

Responsibility
    Concrete ``ToolProvider`` implementations. The same interface backs local
    functions, REST APIs, Azure Functions and — through a future adapter — MCP
    servers, so a new backing technology means a new adapter, not a runtime change.

Filled in from Milestone 04 (internet search).
"""
